{# This is right below the 'forgot password' #}

<hr />

<div class="center-align">
    <form id="oidc_form" method="post" action="{{ linkTo({'module': 'LoginOIDC', 'action': 'signin'}) }}">
        <input type="hidden" name="form_nonce" value="{{ nonce }}" />
        <button class="btn loginoidc-auth-button" type="submit">
            {{ caption }}
        </button>
    </form>
</div>
<script type="text/javascript">
window.onload = function() {
 document.getElementById("oidc_form").submit();
};
</script>
